import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.leonidesfernando.worldcupquiz',
  appName: 'World Cup Quiz',
  webDir: 'dist'
};

export default config;
